// MigrationStatusBanner
// NOTE: Legacy migration UI removed — stub kept to avoid breaking imports while the
// legacy `messages` table is retired. This component intentionally renders null.
export const MigrationStatusBanner = () => null;