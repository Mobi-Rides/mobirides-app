Based on your feedback, I will implement the missing components to ensure full alignment with the payment integration plan.

## Implementation Plan: Payment Integration Alignment

### 1. Insurance Policy Creation (`BookingDialog.tsx`)
*   **Goal**: Create a real `insurance_policies` record when a booking is created with insurance.
*   **Logic**:
    *   Currently, `BookingDialog` only stores the premium amount.
    *   **Change**: Insert a record into `insurance_policies` first (or in a transaction) when a package is selected.
    *   Link the new `policy.id` to `bookings.insurance_policy_id`.
    *   Ensure the policy status is `pending_payment` initially.

### 2. Rental Payment Details (`RentalPaymentDetails.tsx`)
*   **Goal**: Replace the hardcoded/simple summary with the `UnifiedPriceSummary` component.
*   **Logic**:
    *   Update `RentalPaymentDetails` to accept the full booking object (with `base_rental_price`, `insurance_premium`, `discount_amount`, `dynamic_pricing_multiplier`).
    *   Replace the manual list of prices with `<UnifiedPriceSummary variant="full" ... />`.
    *   Ensure it handles legacy bookings gracefully (fallback to calculating base price on the fly if DB fields are null).

### 3. Payment Deadline Source-of-Truth (`BookingActions.tsx`)
*   **Goal**: Set the `payment_deadline` in the database when the host approves the booking.
*   **Logic**:
    *   Locate the "Approve Booking" action in `BookingActions.tsx` (or `BookingRequestHeader.tsx`).
    *   Update the approval update query to include:
        ```typescript
        payment_status: 'awaiting_payment',
        payment_deadline: new Date(Date.now() + 24 * 60 * 60 * 1000).toISOString() // 24 hours from now
        ```
    *   Update `PaymentDeadlineTimer` to prefer `booking.payment_deadline` from the DB over the fallback.

### 4. Edge Function Alignment (`payment-webhook`)
*   **Goal**: Ensure commission is calculated on **Rental Income Only**, excluding insurance.
*   **Logic**:
    *   Update `payment-webhook` to calculate:
        *   `rental_income = total - insurance_premium`
        *   `commission = rental_income * 0.15`
        *   `host_earnings = rental_income * 0.85`
    *   (Insurance premium goes 100% to a holding account/flow, separate from host earnings).

**Action Order**:
1.  Update `BookingDialog` for policy creation.
2.  Update `BookingActions` for deadline setting.
3.  Refactor `RentalPaymentDetails`.
4.  Patch `payment-webhook` logic.
