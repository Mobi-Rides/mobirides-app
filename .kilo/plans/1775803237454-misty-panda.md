# Fix Unused Import in AdvancedUserManagement.tsx

## Issue
Unused import warning for `useEffect` in `src/components/admin/AdvancedUserManagement.tsx`

## Root Cause
The file imports `useEffect` from React but accesses it via `React.useEffect` instead of the imported `useEffect`, making the import unused.

## Solution
Remove the unused `useEffect` from the React import while keeping the functionality intact by continuing to use `React.useEffect`.

## Changes Required
**File:** `src/components/admin/AdvancedUserManagement.tsx`
**Line 1:** Change import from:
```diff
-import React, { useState, useEffect } from "react";
+import React, { useState } from "react";
```

## Verification
- The `useEffect` hook is still used via `React.useEffect` on line 167
- The `useState` hook is still used (visible in the code)
- No runtime behavior changes
- Removes the unused import warning

## Safety
This change only removes an unused import and does not alter any functionality, dependencies, or behavior.